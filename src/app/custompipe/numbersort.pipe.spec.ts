import { NumbersortPipe } from './numbersort.pipe';

describe('NumbersortPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbersortPipe();
    expect(pipe).toBeTruthy();
  });
});
